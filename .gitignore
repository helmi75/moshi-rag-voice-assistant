# Variables d'environnement
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
pip-log.txt
pip-delete-this-directory.txt
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/

# Virtual environments
venv/
ENV/
env/
.venv
.modal-venv/

# Clone local du dépôt de voix Kyutai (kyutai/tts-voices) : sert à écouter les
# extraits avant de les proposer dans l'admin. Les voix servies, elles, sont
# embarquées dans l'image Modal (deploy/modal_moshi_server.py:VOICE_FOLDERS).
tts-voices/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Modèles et données (volumes Docker)
volumes/models/
volumes/data/
*.ckpt
*.safetensors
*.bin
*.pt
*.pth
*.onnx

# Logs
*.log
logs/
*.log.*

# Docker
.dockerignore
docker-compose.override.yml

# Caddy
caddy_data/
caddy_config/

# Fichiers temporaires
*.tmp
*.temp
.cache/

# Base de données
*.db
*.sqlite
*.sqlite3

# Archives (sauf celles nécessaires)
*.zip
*.tar.gz
*.rar
!projet-moshi-vast.zip

# OS
Thumbs.db
.DS_Store


# Snapshot local de l'outil de coût
.cost_snapshot.json

# Données locales (musiques uploadées, DB locale)
data/

# Fichiers de travail personnels, jamais versionnés. Le dépôt est PUBLIC : un
# `git add -A` distrait suffirait à publier des identifiants collés dans un
# bloc-notes. Ranger ce genre de fichier dans local/ plutôt qu'à la racine.
local/
/twillo.txt
/outils.sh

# Audio du banc d'essai : une piste appelant réelle, jamais versionnée (données personnelles).
banc.ulaw
*.ulaw
